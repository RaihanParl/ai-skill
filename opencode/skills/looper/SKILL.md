---
name: looper
description: >
  Design and scaffold review-gated agent loops with best-practice coaching.
  Use when user wants agent loop design, iterative workflow, self-review loop,
  LLM-as-judge loop, multi-model council, or reviewer gate.
---

Looper skill installed from `~/.config/opencode/skills/looper`.
Read `SKILL.md` first and follow it exactly.

Install looper: `git clone <repo-url> ~/.config/opencode/skills/looper`

Use `~/.config/opencode/skills/looper` as `LOOPER_ROOT` for scripts and assets.
